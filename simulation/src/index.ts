import { vec2 } from 'gl-matrix';

export class SimulationEngine {
    constructor() {
        console.log("Simulation Engine Initialized");
    }

    update(dt: number) {
        // Core update loop
    }
}
